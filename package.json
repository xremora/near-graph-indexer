{
  "name": "test-near",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ xremora/test-near",
    "create-local": "graph create --node http://localhost:8020/ xremora/test-near",
    "remove-local": "graph remove --node http://localhost:8020/ xremora/test-near",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xremora/test-near",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.0",
    "@graphprotocol/graph-ts": "0.28.1"
  }
}
